fn main() {
    let mut counter = 0;
    // Loop runs until counter reaches 5.
    while counter < 5 {
        counter += 1;
        // Print current value to verify logic.
        println!("Count: {}", counter);
    }
}
